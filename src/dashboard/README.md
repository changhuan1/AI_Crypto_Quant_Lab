# Dashboard

Run:

```powershell
streamlit run src/dashboard/app.py
```

The dashboard reads the local DuckDB database and shows:

- portfolio NAV and drawdown
- latest A-share and crypto signals
- Shanghai Composite constituent breadth
- data coverage
