[project]
name = "ai-crypto-quant-lab"
version = "0.1.0"
description = "Research-first AI market and crypto spot quantitative lab."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
