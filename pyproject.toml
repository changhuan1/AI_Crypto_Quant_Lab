[project]
name = "ai-crypto-quant-lab"
version = "1.0.0"
description = "Local quant research and strategy-hosting platform for A-share and crypto strategies."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
